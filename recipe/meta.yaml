{% set name = "cellfinder-napari" %}
{% set version = "0.0.20" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/cellfinder-napari-{{ version }}.tar.gz
  sha256: c5e3665eb87ffdc5abec99fdc9cbd493752c5f29689b5e9d2b6ea83b65ee1aed

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - brainreg
    - brainglobe-napari-io
    - cellfinder-core >=0.2.4
    - cellfinder-napari
    - configparser
    - fancylog >=0.0.7
    - imio
    - imlib >=0.0.26
    - napari
    - brainglobe-napari-io
    - multiprocessing-logging
    - napari-ndtiffs
    - napari-plugin-engine >=0.1.4
    - natsort
    - numpy
    - packaging
    - pandas
    - psutil
    - python >=3.7
    - scikit-image
    - scikit-learn
    - slurmio >=0.0.4
    - tensorflow >=2.5.0
    - tifffile
    - tqdm

test:
  imports:
    - cellfinder_napari
  commands:
    - pip list
    # - pip check
  requires:
    - pip
    - oclgrind

about:
  home: https://brainglobe.info/cellfinder
  summary: Efficient cell detection in large images
  license: BSD-3-Clause
  license_file: LICENSE
  dev_url: https://github.com/brainglobe/cellfinder

extra:
  recipe-maintainers:
    - adamltyson
    - goanpeca
    - jaimergp
